// const eventSource = new EventSource('http://localhost:3000/event-stream');

// eventSource.addEventListener('message', (event) => {
//   const eventData = JSON.parse(event.data);
//   console.log('Received message event:', eventData);
// });

// eventSource.addEventListener('error', (event) => {
//   console.error('An error occurred:', event);
// });

// eventSource.addEventListener('open', (event) => {
//   console.log('Connection to the server is open.');
// });

// eventSource.addEventListener('close', (event) => {
//   console.log('Connection to the server is closed.', event);
// });
console.log("hello");
